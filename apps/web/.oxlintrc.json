{
  "$schema": "../../node_modules/oxlint/configuration_schema.json",
  "extends": ["../../.oxlintrc.base.json"],
  "plugins": ["jsx-a11y", "react"],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["@/features/*/*"],
            "message": "Import a feature through its barrel: @/features/<name>. Inside a feature, use relative ./ imports."
          },
          {
            "group": ["@features/*", "@pages/*", "@components/*", "@utils/*"],
            "message": "Alias removed — use @/ (e.g. @/features/records)."
          },
          {
            "group": ["../../**"],
            "message": "Don't climb more than one folder up — use @/ for anything further, ./ for siblings. One level up is allowed so a sub-domain folder (records/versions/) can reach its parent feature."
          }
        ]
      }
    ]
  }
}
