# Not built. Holds the semgrep image ref for .github/workflows/security.yml so
# Dependabot's docker ecosystem can bump tag + digest (it can't update
# `container.image` in workflow files directly).
FROM semgrep/semgrep:1.176.1@sha256:34ab619bf1391a24bfda3f05debd0d8a6ce3093c2d5f9d39cfc00f83c1397823
